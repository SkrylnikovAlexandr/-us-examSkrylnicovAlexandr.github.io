nav {
  background-color: #343a40;
  padding: 15px 20px;
}

.nav-content {
  display: flex;
  align-items: center;
  justify-content: space-between;
  max-width: 1000px;
  margin: 0 auto;
}

.logo {
  height: 50px;
  width: auto;
  margin-right: 20px;
}

.nav-links {
  display: flex;
  gap: 15px;
}

.nav-links a {
  color: white;
  text-decoration: none;
  font-size: 18px;
  padding: 10px 15px;
  border-radius: 5px;
  transition: background 0.3s;
}

.nav-links a:hover {
  background-color: #f8d210;
  color: white; /* Чтобы текст при наведении оставался белым */
}

/* Стили для изображения профиля */
#profile-photo {
  width: 200px; /* Устанавливаем ширину */
  height: auto; /* Автоматическая высота для сохранения пропорций */
  border-radius: 10px; /* Скругленные углы */
  margin-bottom: 15px; /* Отступ снизу */
  box-shadow: 0 4px 10px rgba(0, 0, 0, 0.2); /* Тень для эффекта */
  transition: transform 0.3s ease-in-out; /* Плавный эффект при изменении состояния */
}

#profile-photo:hover {
  transform: scale(1.1); /* Увеличиваем фото при наведении */
}
#like-dislike-container {
  margin-top: 20px;
  display: flex;
  gap: 15px;
}

#like-btn,
#dislike-btn {
  padding: 10px 15px;
  border: none;
  border-radius: 6px;
  font-size: 16px;
  cursor: pointer;
  background-color: #e0e0e0;
  transition: background-color 0.3s;
}

#like-btn:hover {
  background-color: #c8e6c9;
}

#dislike-btn:hover {
  background-color: #ffcdd2;
}
